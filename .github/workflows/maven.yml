name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build with Maven
      # Passo 1: Executa o build na pasta correta. ISTO ESTÁ CORRETO.
      working-directory: ./designpattern 
      run: mvn -B package --file pom.xml

    - name: Update dependency graph
      # Passo 2: CORREÇÃO - Usamos 'working-directory' para este passo também.
      # Removemos o 'with:' que não é válido e usamos a abordagem correta.
      working-directory: ./designpattern
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
